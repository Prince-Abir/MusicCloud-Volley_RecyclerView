<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.musicplayer">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:fullBackupOnly="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicPlayer">


        <!--        Payment Section-->
        <activity
            android:name=".Payment.PaymentActivity"
            android:exported="false"
            android:theme="@style/custom_action_bar">

        </activity>


        <!-- This is Splash Activity & Launcher Activity -->
        <activity
            android:name=".SplashScreen"
            android:exported="true"
            android:theme="@style/custom_action_bar_splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- This is Hindi Song Section -->
        <activity
            android:name=".AllSections.HindiSong"
            android:exported="false"
            android:parentActivityName=".MainActivity">

        </activity>


        <!-- This is Bangla Song Section -->
        <activity
            android:name=".AllSections.BanglaSong"
            android:exported="false"
            android:parentActivityName=".MainActivity">

        </activity>


        <!-- This is MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/custom_action_bar">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>